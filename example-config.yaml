# Example configuration file for claude-skills
# Copy this to ~/.config/claude-skills/config.yaml and customize

# Directory paths
personal_skills_dir: ~/.claude/skills
personal_commands_dir: ~/.claude/commands
project_skills_dir: .claude/skills
project_commands_dir: .claude/commands

# Path to the claude-skills repository (for browsing and installing skills)
# Set this to your local clone of the repository
repository_path: /path/to/claude-skills

# Default installation target (personal or project)
default_target: personal

# Platform-specific directories
platforms:
  claude:
    skills_dir: ~/.claude/skills
    commands_dir: ~/.claude/commands

  codex:
    skills_dir: ~/.config/openai/skills
    commands_dir: ~/.config/openai/commands

  gemini:
    skills_dir: ~/.config/gemini/skills
    commands_dir: ~/.config/gemini/commands

# Additional options (optional)
# verbose: false
# auto_update: false
